<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contest Page - CodeComp</title>
    <link rel="stylesheet" href="/css/contest.css">
</head>
<body>

    <header>
        <nav class="container">
            <!-- <a href="/" class="logo">CodeWars</a> -->
            <div class="contest-timer">Time Left: <strong>03:00:00</strong></div>
            <ul>
                <li>
                    <button id="theme-toggle" class="theme-toggle-btn" onclick="toggleTheme()" aria-label="Toggle dark mode">
                        <span class="theme-icon">🌙</span>
                    </button>
                </li>
                <li><button id="end-session-btn" class="btn btn-secondary">End Session</button></li>
            </ul>
        </nav>
    </header>

    <div class="contest-container">
        
        <aside class="problem-sidebar">
            <div class="problem-list">
                <h3><%= difficulty.charAt(0).toUpperCase() + difficulty.slice(1) %> Level Problems</h3>
                <ul id="problem-list">
                    <li class="placeholder">Loading problems...</li>
                </ul>
            </div>

            <div class="problem-statement" id="problem-statement">
                <h2>Loading...</h2>
                <p>Select a problem to begin.</p>
            </div>
        </aside>

        <main class="code-sidebar">
            <div class="code-editor-container">
                <div class="editor-header">
                    <label for="language">Language:</label>
                    <select id="language" name="language">
                        <option value="python">Python</option>
                        <option value="java">Java</option>
                        <option value="c">C</option>
                    </select>
                    <div class="navigation-buttons" style="display:inline-block;margin-left:1rem;">
                        <button id="prev-question-btn" class="btn btn-secondary" style="margin-right:0.5rem;" disabled>Previous Question</button>
                        <button id="next-question-btn" class="btn btn-secondary">Next Question</button>
                    </div>
                </div>             
                <textarea id="code-editor" spellcheck="false">
# Write your code here
def solve():
    pass
                </textarea>
                
                <div class="editor-footer">
                    <button id="run-code-btn" class="btn btn-secondary">Run Code</button>
                    <button id="submit-code-btn" class="btn btn-primary">Submit Solution</button>
                </div>
            </div>

            <div class="submission-status">
                <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 1rem;">
                    <h3>Output & Answer</h3>
                    <button id="clear-output-btn" class="btn btn-secondary" style="padding: 0.5rem 1rem; font-size: 0.9rem;">Clear</button>
                </div>
                
                <div id="output-content">
                    <p class="output-placeholder">Output will appear here...</p>
                </div>

                <div id="answer-section" style="display: none; margin-top: 1.5rem; padding-top: 1.5rem; border-top: 1px solid var(--border-color);">
                    <label for="answer-input" style="display: block; margin-bottom: 0.5rem; font-weight: 600;">Problem Answer/Algorithm (for Easy level):</label>
                    <textarea id="answer-input" placeholder="Type your answer or guess the algorithm here..." style="width: 100%; min-height: 100px; padding: 0.75rem; border: 1px solid var(--border-color); border-radius: 5px; font-family: inherit; resize: vertical;"></textarea>
                </div>
            </div>
        </main>
    </div>

    <!-- Contest Ended Modal -->
    <div id="contest-ended-modal" class="modal-overlay" style="display: none;">
        <div class="modal-content">
            <h2>Contest Has Ended</h2>
            <p>Thank you for participating in the contest!</p>
            <p>We would love to hear your feedback about your experience.</p>
            <button class="btn btn-primary" onclick="window.location.href='/feedback'">
                Share Your Feedback
            </button>
        </div>
    </div>

    <!-- Disclaimer Modal -->
    <div id="disclaimer-modal" class="modal-overlay" style="display: flex;">
        <div class="modal-content" style="max-width: 600px;">
            <div class="modal-header" style="text-align: center; margin-bottom: 1.5rem;">
                <h2 style="color: var(--primary-color); margin: 0;">⚠️ Important Disclaimer</h2>
            </div>

            <div class="disclaimer-body" style="max-height: 60vh; overflow-y: auto; margin-bottom: 1.5rem;">
                <h3 style="color: var(--text-color); margin-top: 0;">Cheating Detection & Contest Rules</h3>
                
                <p style="color: var(--text-color); line-height: 1.6;">
                    This contest platform is equipped with advanced cheating detection mechanisms to ensure fairness and integrity for all participants. By proceeding, you acknowledge that:
                </p>

                <div style="background-color: var(--surface-color); padding: 1rem; border-radius: 5px; margin: 1rem 0; border-left: 4px solid var(--danger-color);">
                    <h4 style="color: var(--text-color); margin-top: 0;">🔍 Monitoring & Tracking</h4>
                    <ul style="color: var(--text-muted); margin: 0.5rem 0;">
                        <li>Your activity will be monitored throughout the contest</li>
                        <li>Any suspicious behavior will be recorded and logged</li>
                        <li>Violations may result in disqualification</li>
                    </ul>
                </div>

                <div style="background-color: var(--surface-color); padding: 1rem; border-radius: 5px; margin: 1rem 0; border-left: 4px solid var(--primary-color);">
                    <h4 style="color: var(--text-color); margin-top: 0;">🚫 Prohibited Actions</h4>
                    <p style="color: var(--text-muted); margin: 0.5rem 0;"><strong>You will be flagged for:</strong></p>
                    <ul style="color: var(--text-muted); margin: 0.5rem 0;">
                        <li><strong>Tab Switching:</strong> Leaving the contest tab or switching to another window</li>
                        <li><strong>Window Blur:</strong> Clicking outside the contest browser window</li>
                        <li><strong>Right-Click:</strong> Attempting to use right-click context menu</li>
                        <li><strong>Fullscreen Exit:</strong> Exiting fullscreen mode during the contest</li>
                    </ul>
                </div>

                <div style="background-color: var(--surface-color); padding: 1rem; border-radius: 5px; margin: 1rem 0; border-left: 4px solid #28a745;">
                    <h4 style="color: var(--text-color); margin-top: 0;">✅ What You Can Do</h4>
                    <ul style="color: var(--text-muted); margin: 0.5rem 0;">
                        <li>Write and test code in the provided editor</li>
                        <li>Switch between problems using the navigation buttons</li>
                        <li>Copy and paste code snippets from problem statements</li>
                        <li>Use the browser's built-in developer tools with caution</li>
                        <li>Review problem statements and examples</li>
                        <li>Submit solutions through the Submit button</li>
                    </ul>
                </div>

                <div style="background-color: var(--surface-color); padding: 1rem; border-radius: 5px; margin: 1rem 0; border-left: 4px solid #ffc107;">
                    <h4 style="color: var(--text-color); margin-top: 0;">⏱️ Contest Rules</h4>
                    <ul style="color: var(--text-muted); margin: 0.5rem 0;">
                        <li>Contest duration is strictly enforced</li>
                        <li>All submissions are timestamped and logged</li>
                        <li>Multiple cheating violations may result in immediate disqualification</li>
                        <li>Decisions made by the organizing committee are final</li>
                        <li>By participating, you agree to abide by these rules</li>
                    </ul>
                </div>

                <p style="color: var(--text-muted); font-style: italic; margin-top: 1.5rem;">
                    <strong>Note:</strong> This system is designed to maintain fair competition. If you experience any technical issues, please contact the organizing committee immediately.
                </p>
            </div>

            <div style="display: flex; gap: 1rem; justify-content: center;">
                <button class="btn btn-primary" onclick="acceptDisclaimer()" style="flex: 1; max-width: 200px;">
                    Accept & Start Contest
                </button>
            </div>
        </div>
    </div>

    <script src="/js/darkmode.js"></script>
    <script src="/js/timer_contest.js"></script>
    <script src="/js/contest.js"></script>
    <script>
        // ============= DISCLAIMER HANDLER =============
        function acceptDisclaimer() {
            const modal = document.getElementById('disclaimer-modal');
            modal.style.display = 'none';
            document.body.style.overflow = 'auto';
            // Store acceptance in sessionStorage (cleared when browser closes)
            sessionStorage.setItem('disclaimerAccepted', 'true');
            // Record the time when user accepted the disclaimer (actual contest start)
            sessionStorage.setItem('disclaimerAcceptedTime', Date.now().toString());
            console.log('✅ Disclaimer accepted - Contest started');
            
            // Start the contest timer when user accepts disclaimer
            if (window.startTimer && typeof window.startTimer === 'function') {
                console.log('⏱️ Starting contest timer...');
                window.startTimer();
            } else {
                console.warn('⚠️ Timer function not available yet');
            }
        }

        function rejectDisclaimer() {
            // Function removed - users must accept to continue
            // Decline & Exit button has been removed from the disclaimer
        }

        // Show disclaimer on page load if not accepted in this session
        window.addEventListener('load', function() {
            const disclaimerAccepted = sessionStorage.getItem('disclaimerAccepted');
            const modal = document.getElementById('disclaimer-modal');
            
            if (!disclaimerAccepted && modal) {
                modal.style.display = 'flex';
                document.body.style.overflow = 'hidden';
                console.log('⚠️ Showing disclaimer modal');
            } else if (modal) {
                modal.style.display = 'none';
                document.body.style.overflow = 'auto';
                // If already accepted this session, start the timer
                if (disclaimerAccepted && window.startTimer && typeof window.startTimer === 'function') {
                    console.log('⏱️ Restarting contest timer after page reload...');
                    window.startTimer();
                }
            }
        });

        // Prevent users from skipping disclaimer with keyboard shortcuts
        document.addEventListener('keydown', function(e) {
            const modal = document.getElementById('disclaimer-modal');
            if (modal && modal.style.display === 'flex') {
                // Prevent Escape key from closing the modal
                if (e.key === 'Escape') {
                    e.preventDefault();
                    console.warn('⚠️ Escape key pressed - Disclaimer must be accepted or rejected');
                }
            }
        });
    </script>
    <script>
        // ============= CHEATING DETECTION MECHANISM =============
        let isCheatingDetectionActive = true;
        let cheatingCount = 0;

        // Detect when user leaves the page/tab
        document.addEventListener('visibilitychange', function() {
            if (isCheatingDetectionActive) {
                if (document.hidden) {
                    // User switched tab/minimized window
                    console.warn('⚠️ User left the contest page');
                    cheatingCount++;
                    recordCheating('tab_switch');
                } else {
                    console.log('✓ User returned to contest page');
                }
            }
        });

        // Detect when window loses focus
        window.addEventListener('blur', function() {
            if (isCheatingDetectionActive && !document.hidden) {
                // User clicked outside the window but tab is still visible
                console.warn('⚠️ User window lost focus');
                cheatingCount++;
                recordCheating('window_blur');
            }
        });

        // Detect full-screen exit
        document.addEventListener('fullscreenchange', function() {
            if (isCheatingDetectionActive && !document.fullscreenElement) {
                console.warn('⚠️ User exited full-screen mode');
                cheatingCount++;
                recordCheating('fullscreen_exit');
            }
        });

        // Detect right-click (trying to inspect/copy)
        document.addEventListener('contextmenu', function(e) {
            if (isCheatingDetectionActive) {
                e.preventDefault();
                console.warn('⚠️ Right-click attempt detected');
                cheatingCount++;
                recordCheating('right_click');
                alert('Right-click is disabled during the contest');
                return false;
            }
        });

        // Note: Copy/paste detection removed to allow users to copy code snippets from problems

        // Function to record cheating incident
        async function recordCheating(type) {
            try {
                const response = await fetch('/api/record-cheating', {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    credentials: 'include',
                    body: JSON.stringify({
                        cheatingType: type,
                        timestamp: new Date().toISOString()
                    })
                });

                if (response.ok) {
                    console.log('✓ Cheating incident recorded');
                } else {
                    console.error('Failed to record cheating incident');
                }
            } catch (error) {
                console.error('Error recording cheating:', error);
            }
        }

        // Override End Session button to disable cheating detection
        document.addEventListener('DOMContentLoaded', function() {
            const endSessionBtn = document.getElementById('end-session-btn');
            if (endSessionBtn) {
                const originalOnClick = endSessionBtn.onclick;
                endSessionBtn.addEventListener('click', async function(e) {
                    // Disable cheating detection
                    isCheatingDetectionActive = false;
                    console.log('✓ Cheating detection disabled');
                });
            }
        });

        // Disable cheating detection when page unloads (after session ends)
        window.addEventListener('beforeunload', function() {
            isCheatingDetectionActive = false;
        });
    </script>
    <script>
        // End Session button handler
        document.addEventListener('DOMContentLoaded', function() {
            const endSessionBtn = document.getElementById('end-session-btn');
            if (endSessionBtn) {
                endSessionBtn.addEventListener('click', async function() {
                    // Confirm action
                    if (confirm('Are you sure you want to end your contest session? You will be redirected to the feedback page.')) {
                        // Disable cheating detection
                        isCheatingDetectionActive = false;
                        
                        try {
                            // Call endpoint to mark contest as ended
                            const response = await fetch('/end-contest-session', {
                                method: 'POST',
                                headers: {
                                    'Content-Type': 'application/json'
                                }
                            });

                            const data = await response.json();
                            
                            if (response.ok) {
                                // Show the contest ended modal
                                if (typeof showContestEndedModal === 'function') {
                                    showContestEndedModal();
                                } else {
                                    // Fallback if function not available yet
                                    const modal = document.getElementById('contest-ended-modal');
                                    if (modal) {
                                        modal.style.display = 'flex';
                                        document.body.style.overflow = 'hidden';
                                    }
                                }
                            } else {
                                alert('Error ending session. Please try again.');
                            }
                        } catch (error) {
                            console.error('Error ending session:', error);
                            // Still show modal even if request fails
                            if (typeof showContestEndedModal === 'function') {
                                showContestEndedModal();
                            } else {
                                const modal = document.getElementById('contest-ended-modal');
                                if (modal) {
                                    modal.style.display = 'flex';
                                    document.body.style.overflow = 'hidden';
                                }
                            }
                        }
                    }
                });
            }
        });
    </script>
</body>
</html>
